# dsh-diorama bundle patch: inserts its dsh.client row into the web plugin
# roster. Install with `dsh plugin --profile web add <path-or-git-url>`.
- insert:
    - id: ui-skin-diorama
      name: 'dsh-diorama'
